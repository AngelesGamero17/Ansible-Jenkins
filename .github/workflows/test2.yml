name: Ejecutar Máquina Virtual en AWS

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del código
      uses: actions/checkout@v4

    - name: Ejecutar Máquina Virtual en AWS
      run: ssh -i ${{ secrets.CORAIMA_KEY}} prueba2@18.220.141.63

