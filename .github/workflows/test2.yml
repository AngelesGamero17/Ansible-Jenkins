name: Ejecutar Máquina Virtual en AWS

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del código
      uses: actions/checkout@v4

    - name: Ejecutar Máquina Virtual en AWS
      run: ssh -i /kafka/.github/workflows/coraima.pem prueba2@18.220.141.63
